<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anybirth.antbirth_setting">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SettingMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SettingUserActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SettingUserActivity" />
        </activity>

        <activity android:name=".SettingUserPhoneActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SettingUserActivity" />
        </activity>

        <activity android:name=".SettingUserPasswordActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SettingUserActivity" />
        </activity>

        <activity android:name=".SettingUserSingoutActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SettingUserActivity" />
        </activity>

        <activity android:name=".SettingAlramActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SettingUserActivity" />
        </activity>

        <activity android:name=".SettingNoticeActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SettingUserActivity" />
        </activity>

        <activity android:name=".SettingPolicyActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SettingUserActivity" />
        </activity>

        <activity android:name=".SettingPolicyCompanyActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SettingUserActivity" />
        </activity>
    </application>

</manifest>